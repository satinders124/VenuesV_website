{
  "name": "venuesv-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "check": "node scripts/check.mjs && html-validate public/*.html",
    "dev": "vercel dev",
    "build": "npm run check"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.109.0",
    "stripe": "^22.2.0"
  },
  "devDependencies": {
    "html-validate": "^10.12.0",
    "vercel": "^50.22.0"
  }
}
